
//type (x: number, y:number)-> number;
const add=( a, b )=> a+b;
//type (x: number, y:number)->number;
const substract = (a, b)=> a -b;
//
// type (x:number, y: number)->number;
const multiply = (a ,b) =>{ return a *b
}; 
// type (x: number, y:number)->number;
const divide =(a, b)=> a / b;{
  if(b===0) return;
  return a/b;}

  const divide_for_premium=(a, b)=> a/b;
  
//git init 로컬 레포지토리 만들기
//git add 스테이징으로 옮기기
//git comit 로컬레포지토리 저장하기
//git push  로컬레포지토리와 리모트 레포지토리 동기화 하기
